- 정렬 쿼리 $() sql injection 처리

- url 네이밍

- throws 사용 이유 
- ckecked exception , unckecked exception 차이

-fileservice를 boardservice에 위임

- xml 파일은 소문자로, 구분은  ' - ' 로 작성 

- contentNum은 선택할수있게 select box

- throw new RuntimeException( error ) ?

- 삭제파일시퀀스 리스트를 쿼리스트링으로 보내서 처리하라
컨트롤러에서는 @PathVariable 로 받아서 처리

- post를 put 과 delete로 바꿔서 처리

- api return값에 대한 스펙(규격)을 정의해서 해당 스펙으로만 리턴하도록
( 리턴 형태는 json으로 )

- http header와 body의 정의

- Bean으로 만들어주는 이유!! ( static으로 만들거나, bean으로 만들어서 싱글톤패턴으로 사용하거나 )

--------------------------------------------------------
미영씨
- 역할과 책임 ( 하나의 메소드에는 하나의 기능)

- uri 네이밍은 소문자,  ' - '(dash)로 구분, camel x

- 